crear_frase <- function (x){
  if (x == error) {
    elementos <- cbind(sample(exclamacion_negativa), sample(frase_negativa))
    if (elementos[,2]=="mejor vamos por") {}
  } else if ( x== exito ){ }
  x
}
 sample(refran,)